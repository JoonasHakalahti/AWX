---
- name: Test SSH connection to ProCurve
  hosts: procurve-2910
  gather_facts: no
  connection: network_cli

  vars:
    ansible_network_os: community.network.procurve

  tasks:
    - name: Run 'show vlan'
      ansible.netcommon.cli_command:
        command: show vlan